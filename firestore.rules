rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // All writes & reads go through the Firebase Admin SDK in API routes,
    // so direct client access is denied.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
